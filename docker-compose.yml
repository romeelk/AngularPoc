version: '2.2'

services:
    app:
        image: 'angulardockerapp'
        build: '.'
        ports:
            - 3000:80
       
    api:
        image: 'productapi'
        build: 
            dockerfile: Dockerfile
            context: ./src/api/productsapi
        ports:
            - 5000:80
        